export type ResolverParent = unknown;
export type ResolverContext = { currentUser: any };

export interface JwtPayloadInterface {
  id: string;
}

export interface JwtPayloadInterface {
  id: string;
}

export interface IError {
  message: string;
}
